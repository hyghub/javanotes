spring:
  application:
    name: config-server
  cloud:
    config:
      label: master
      server:
        git:
          uri: https://github.com/hyghub/springcloudConfig/
          search-paths: respo
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
